PORT=3000
HOST=0.0.0.0
# Recommended in production: 32-byte random key encoded as base64.
# Example generation: openssl rand -base64 32
NUTE_MASTER_ENCRYPTION_KEY=
# Set to 1 only behind HTTPS/reverse proxy.
COOKIE_SECURE=0
